<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:DelCorp.ViewModels"
             xmlns:models="clr-namespace:DelCorp.Models"
             xmlns:converters="clr-namespace:DelCorp.Converters" x:Class="DelCorp.Views.RegistrarEtapaPage"
             x:DataType="viewmodels:RegistrarEtapaViewModel"
             Title="Registrar Etapas del Presupuesto">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:InverseBoolConverter x:Key="InverseBoolConverter" />
            <converters:CountToBoolConverter x:Key="CountToBoolConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Spacing="10" Padding="20">

            <Label Text="Nombre de la Etapa/Actividad" FontAttributes="Bold"/>
            <Frame Padding="0" CornerRadius="8" BorderColor="LightGray">
                <Entry Placeholder="Escriba para buscar o crear..."
                       Text="{Binding SearchTextActividad, Mode=TwoWay}"
                       ClearButtonVisibility="WhileEditing"
                       VerticalOptions="Center"/>
            </Frame>

            <CollectionView ItemsSource="{Binding ActividadesDisponibles}"
                            SelectionMode="None"
                            MaximumHeightRequest="150"
                            IsVisible="{Binding ActividadesDisponibles.Count, Converter={StaticResource CountToBoolConverter}}">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Actividad">
                        <StackLayout Padding="10" Spacing="5">
                            <Label Text="{Binding NombreActividad}" FontSize="16"/>
                            <StackLayout.GestureRecognizers>
                                <TapGestureRecognizer 
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:RegistrarEtapaViewModel}}, Path=SelectActividadCommand}"
                                    CommandParameter="{Binding .}"/>
                            </StackLayout.GestureRecognizers>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <VerticalStackLayout IsVisible="{Binding ShowNewActividadFields}" Spacing="10">
                <Label Text="Registrar Nueva Actividad" FontAttributes="Bold" TextColor="#512BD4"/>
                <Picker Title="Seleccione una Categoría"
                        ItemsSource="{Binding CategoriasActividad}"
                        ItemDisplayBinding="{Binding NombreCategoriaActividad}"
                        SelectedItem="{Binding SelectedCategoriaActividad}"/>
                <Picker Title="Seleccione Unidad de Medida"
                        ItemsSource="{Binding DisponibleUniMedRe}"
                        ItemDisplayBinding="{Binding NombreUniMedRe}"
                        SelectedItem="{Binding SelectedUniMedRe}"/>
            </VerticalStackLayout>

            <Label Text="Cantidad" FontAttributes="Bold" Margin="0,15,0,0"/>
            <Frame Padding="0" CornerRadius="8" BorderColor="LightGray">
                 <Entry Placeholder="Ej: 100" Keyboard="Numeric" Text="{Binding CantidadEtapa}" />
            </Frame>

            <Button Text="Guardar Etapa" 
                    Command="{Binding GuardarEtapaCommand}" 
                    IsEnabled="{Binding IsNotBusy}"
                    Margin="0,20,0,0"/>

            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />


            <Label Text="Etapas Registradas" FontAttributes="Bold" FontSize="Medium" Margin="0,20,0,0"/>

            <Button Text="Guardar Orden de Etapas"
                    Command="{Binding SaveOrderChangesCommand}"
                    IsVisible="{Binding HasPendingOrderChanges}"
                    IsEnabled="{Binding IsNotBusy}"
                    BackgroundColor="OrangeRed" TextColor="White"
                    Margin="0,5,0,5"/>

            <CollectionView ItemsSource="{Binding Etapas}">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Etapa">
                        <Frame Padding="10" Margin="0,5" CornerRadius="5" BorderColor="LightGray">
                            <Grid ColumnDefinitions="*,Auto,Auto,Auto" RowDefinitions="Auto,Auto,Auto,Auto" ColumnSpacing="5">
                                <Label Grid.Row="0" Grid.Column="0" FontAttributes="Bold">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="Etapa "/>
                                            <Span Text="{Binding NumeroEtapa}"/>
                                            <Span Text=": "/>
                                            <Span Text="{Binding Actividad.NombreActividad}" />
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>

                                <Button Grid.Row="0" Grid.Column="1" Text="▲" FontSize="Small" Padding="5"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:RegistrarEtapaViewModel}}, Path=MoveEtapaUpCommand}"
                                        CommandParameter="{Binding .}"/>
                                <Button Grid.Row="0" Grid.Column="2" Text="▼" FontSize="Small" Padding="5"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:RegistrarEtapaViewModel}}, Path=MoveEtapaDownCommand}"
                                        CommandParameter="{Binding .}"/>

                                <Label Grid.Row="1" Grid.Column="0">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="Categoría: " FontAttributes="Italic"/>
                                            <Span Text="{Binding Actividad.CategoriaActividad.NombreCategoriaActividad}"/>
                                            <Span Text=" - U/M: " FontAttributes="Italic"/>
                                            <Span Text="{Binding Actividad.UnidadMedida.NombreUniMedRe}"/>
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>

                                <Label Grid.Row="2" Grid.Column="0">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="Cantidad Etapa: " FontAttributes="Italic"/>
                                            <Span Text="{Binding CantidadEtapa, StringFormat='{0:N2}'}"/>
                                            <Span Text=" - Monto Total: " FontAttributes="Italic"/>
                                            <Span Text="{Binding MontoTotalEtapa, StringFormat='C{0:C}', FallbackValue='0.00'}"/>
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>

                                <Button Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" Text="Gestionar Sub-Etapas y Recursos"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:RegistrarEtapaViewModel}}, Path=RegistrarSubEtapaCommand}"
                                        CommandParameter="{Binding .}" Margin="0,5,0,0"/>
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            <Label Text="No hay etapas registradas para este presupuesto."
                   IsVisible="{Binding Etapas.Count, Converter={StaticResource InverseBoolConverter}}"
                   HorizontalOptions="CenterAndExpand" Margin="0,10"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>